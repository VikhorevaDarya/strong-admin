# Пересчет количества товаров на всех складах

## Когда нужно запускать

После изменения логики подсчета с "количество позиций" на "сумма quantity" необходимо пересчитать все существующие склады.

## Способ 1: Через консоль браузера (рекомендуется)

1. Откройте приложение Strong Admin в браузере
2. Войдите в систему
3. Откройте консоль разработчика (F12 → Console)
4. Выполните следующий код:

```javascript
// Получаем PocketBase клиент
const pb = (await import('./src/api/pocketbase.client.js')).pb;

// Получаем все склады
const warehouses = await pb.collection('warehouses').getFullList();

console.log(`📦 Начинаем пересчет для ${warehouses.length} складов...`);

// Пересчитываем каждый склад
for (const warehouse of warehouses) {
  // Получаем все товары на этом складе
  const products = await pb.collection('products').getFullList({
    filter: `warehouse="${warehouse.id}"`,
    fields: 'id,quantity',
  });

  // Суммируем quantity
  const totalQuantity = products.reduce((sum, product) => {
    return sum + (product.quantity || 0);
  }, 0);

  // Обновляем склад
  await pb.collection('warehouses').update(warehouse.id, {
    products_count: totalQuantity,
  });

  console.log(`✅ ${warehouse.name}: ${totalQuantity} (товаров: ${products.length})`);
}

console.log('🎉 Пересчет завершен! Обновите страницу.');
```

5. Дождитесь завершения (увидите "Пересчет завершен!")
6. Обновите страницу (F5)

## Способ 2: Через импорт утилиты

```javascript
// Импортируем функцию пересчета
const { recalculateAllWarehousesCount } = await import('./src/utils/warehouse.utils.js');

// Запускаем пересчет
await recalculateAllWarehousesCount();

console.log('✅ Пересчет завершен! Обновите страницу.');
window.location.reload();
```

## Способ 3: Автоматический пересчет через Excel импорт

1. Откройте страницу "Товары"
2. Нажмите кнопку "Импорт из Excel"
3. Загрузите любой Excel файл с товарами (можно пустой)
4. После импорта автоматически запустится `recalculateAllWarehousesCount()`

**Примечание:** Этот способ работает, только если в коде импорта добавлен вызов `recalculateAllWarehousesCount()`.

## Проверка результата

После пересчета проверьте:

1. Откройте страницу "Склады"
2. Проверьте колонку "Количество товаров"
3. Значения должны совпадать с суммой quantity всех товаров на складе

**Пример:**

Склад "Главный":
- Товар A: quantity = 5
- Товар B: quantity = 10
- Товар C: quantity = 3

**Ожидаемое значение `products_count`:** 18 (а не 3)

## Логирование

В консоли должны появиться сообщения:

```
📦 Начинаем пересчет для 2 складов...
✅ Главный склад: 18 (товаров: 3)
✅ Дополнительный склад: 25 (товаров: 5)
🎉 Пересчет завершен! Обновите страницу.
```

## Автоматический пересчет в будущем

После первого ручного пересчета система будет автоматически обновлять `products_count` при:

- Создании товара
- Изменении quantity товара
- Перемещении товара на другой склад
- Удалении товара
- Импорте товаров из Excel

Ручной пересчет больше не потребуется!

## Устранение ошибок

### Ошибка: "Cannot find module"

**Решение:** Убедитесь, что файлы существуют:
- `client/src/api/pocketbase.client.ts`
- `client/src/utils/warehouse.utils.ts`

### Ошибка: "Failed to fetch"

**Решение:** Убедитесь, что PocketBase сервер запущен на `http://127.0.0.1:8090`

### Ошибка: "Unauthorized"

**Решение:** Войдите в систему перед выполнением скрипта

### Значения не обновились

**Решение:**
1. Проверьте консоль на ошибки
2. Обновите страницу (F5)
3. Очистите кеш браузера (Ctrl+Shift+R)

## Альтернатива: SQL запрос в PocketBase

Если у вас есть прямой доступ к базе данных PocketBase:

```sql
-- Обнуляем все счетчики
UPDATE warehouses SET products_count = 0;

-- Пересчитываем для каждого склада
-- (требуется выполнить отдельно для каждого склада)
UPDATE warehouses
SET products_count = (
  SELECT COALESCE(SUM(quantity), 0)
  FROM products
  WHERE warehouse = warehouses.id
)
WHERE id = 'WAREHOUSE_ID';
```

**Внимание:** Этот метод требует знания SQL и прямого доступа к БД.
