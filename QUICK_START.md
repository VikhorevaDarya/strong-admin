# 🚀 Быстрый старт

## Поля в PocketBase - Краткая справка

### 📦 Коллекция `products` (Товары)

```
┌─────────────────┬─────────┬──────────────────────┐
│ Поле            │ Тип     │ Настройки            │
├─────────────────┼─────────┼──────────────────────┤
│ type            │ Text    │ Required ✓           │
│ price           │ Number  │ Required ✓, Min: 0   │
│ quantity        │ Number  │ Required ✓, Min: 0   │
│ warehouse_name  │ Text    │ -                    │
└─────────────────┴─────────┴──────────────────────┘
```

**Пример записи:**
```json
{
  "type": "Ноутбук ASUS",
  "price": 65000,
  "quantity": 15,
  "warehouse_name": "Склад №1 Москва"
}
```

---

### 🏢 Коллекция `warehouses` (Склады)

```
┌─────────────────┬─────────┬──────────────────────┐
│ Поле            │ Тип     │ Настройки            │
├─────────────────┼─────────┼──────────────────────┤
│ name            │ Text    │ Required ✓           │
│ address         │ Text    │ Required ✓           │
│ products_count  │ Number  │ Min: 0, Default: 0   │
└─────────────────┴─────────┴──────────────────────┘
```

**Пример записи:**
```json
{
  "name": "Склад №1 Москва",
  "address": "г. Москва, ул. Складская, д. 10",
  "products_count": 150
}
```

---

## ⚙️ Настройка прав доступа (API Rules)

Для **обеих** коллекций установите следующие правила:

```
List/Search rule:  @request.auth.id != ""
View rule:         @request.auth.id != ""
Create rule:       @request.auth.id != ""
Update rule:       @request.auth.id != ""
Delete rule:       @request.auth.id != ""
```

Это означает: доступ только для авторизованных пользователей.

---

## 📝 Пошаговая инструкция

### 1️⃣ Создание коллекции Products

1. В PocketBase админке нажмите **"New collection"**
2. Выберите **"Base collection"**
3. Name: `products`
4. Нажмите **"+ New field"** и добавьте поля:
   - **type** → Text → Required ✓
   - **price** → Number → Required ✓, Min: 0
   - **quantity** → Number → Required ✓, Min: 0
   - **warehouse_name** → Text
5. Перейдите на вкладку **"API Rules"**
6. Установите для всех правил: `@request.auth.id != ""`
7. Нажмите **"Create"**

### 2️⃣ Создание коллекции Warehouses

1. Нажмите **"New collection"**
2. Выберите **"Base collection"**
3. Name: `warehouses`
4. Нажмите **"+ New field"** и добавьте поля:
   - **name** → Text → Required ✓
   - **address** → Text → Required ✓
   - **products_count** → Number → Min: 0, Default: 0
5. Перейдите на вкладку **"API Rules"**
6. Установите для всех правил: `@request.auth.id != ""`
7. Нажмите **"Create"**

### 3️⃣ Создание пользователя

1. Перейдите в коллекцию **"users"**
2. Нажмите **"+ New record"**
3. Заполните:
   - username: `admin`
   - email: `admin@example.com`
   - password: `admin12345678`
   - passwordConfirm: `admin12345678`
4. Нажмите **"Create"**

### 4️⃣ Запуск приложения

```bash
# В одном терминале (корневая папка проекта)
./pocketbase serve

# В другом терминале
cd client
npm start
```

Откройте http://localhost:3000 и войдите с данными:
- Email: `admin@example.com`
- Пароль: `admin12345678`

---

## 🎯 Готово!

Теперь вы можете:
- ✅ Создавать товары
- ✅ Редактировать товары
- ✅ Удалять товары
- ✅ Создавать склады
- ✅ Редактировать склады
- ✅ Удалять склады
- ✅ Искать по типу товара
- ✅ Искать по названию склада
