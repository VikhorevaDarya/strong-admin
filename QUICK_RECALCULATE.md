# Быстрый пересчет складов

## Способ 1: Через готовый скрипт (рекомендуется)

1. Откройте Strong Admin в браузере
2. Войдите в систему
3. Откройте файл `recalculate-script.js`
4. Скопируйте весь код
5. Откройте консоль браузера (F12 → Console)
6. Вставьте код и нажмите Enter
7. Дождитесь завершения
8. Обновите страницу (F5)

## Способ 2: Одной командой (быстрый)

Откройте консоль браузера (F12) и выполните:

```javascript
(async()=>{const pb=(await import('./src/api/pocketbase.client.js')).pb;const w=await pb.collection('warehouses').getFullList();console.log(`📦 Пересчет ${w.length} складов...`);for(const warehouse of w){const p=await pb.collection('products').getFullList({filter:`warehouse="${warehouse.id}"`,fields:'id,quantity,name'});const q=p.reduce((s,pr)=>s+(pr.quantity||0),0);const n=p.map(pr=>pr.name||'Без названия').join(', ');await pb.collection('warehouses').update(warehouse.id,{products_count:q,products_name:n});console.log(`✅ ${warehouse.name}: ${q} (${p.length} товаров)`);}console.log('🎉 Готово! Обновите страницу.');})();
```

## Способ 3: Через утилиту (если уже импортирована)

```javascript
const { recalculateAllWarehousesCount } = await import('./src/utils/warehouse.utils.js');
await recalculateAllWarehousesCount();
window.location.reload();
```

## Проверка результата

После выполнения:

1. Обновите страницу (F5)
2. Откройте "Склады"
3. Проверьте колонку "Количество товаров"
4. Значения должны обновиться

## Если возникла ошибка

### "unauthorized" или "401"
**Решение:** Войдите в систему и попробуйте снова

### "Cannot find module"
**Решение:** Убедитесь, что вы выполняете скрипт на странице Strong Admin (не в PocketBase Admin)

### "Failed to fetch"
**Решение:** Убедитесь, что PocketBase запущен на http://127.0.0.1:8090

### Значения не изменились
**Решение:** 
1. Проверьте консоль на ошибки
2. Очистите кеш (Ctrl+Shift+R)
3. Попробуйте еще раз

## Автоматизация

После первого пересчета вам не нужно делать это снова - система автоматически обновляет оба поля при любых операциях с товарами!
